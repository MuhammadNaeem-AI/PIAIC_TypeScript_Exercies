//5- Famous Quote 2: Repeat Exercise 4, but this time store the famous person’s name in a variable called famous_person. Then compose your message and store it in a new variable called message. Print your message.

// Store the famous person's name in a variable
const famous_person: string = 'Albert Einstein';

// Store the quote in a variable
const famouse_Quote: string = 'A person who never made a mistake never tried anything new.';

// Compose the message
const message_1: string = `${famous_person} once said, "${famouse_Quote}"`;

// Print the message
console.log(message_1);